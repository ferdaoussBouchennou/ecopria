# =============================================================
#  Ecopria — CI/CD
#  Sur toutes les branches  → compiler + tester
#  Sur main seulement       → compiler + tester + Docker Hub
# =============================================================

name: Ecopria CI/CD

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:

  # ─────────────────────────────────────────────────────────
  #  BACKEND — Spring Boot de chaque microservice
  # ─────────────────────────────────────────────────────────
  build-backend:
    name: Backend ${{ matrix.service }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        service:
          - service-auth
          - service-utilisateur
          - service-action
          - service-inscription
          - service-presence
          - service-recompense
          - service-admin
          - service-notification
          - api-gateway

    steps:
      - name: Télécharger le code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Vérifier si le backend a été modifié
        id: changes
        run: |
          git diff --name-only HEAD~1 HEAD | grep "^${{ matrix.service }}/backend/" \
          && echo "changed=true" >> $GITHUB_OUTPUT \
          || echo "changed=false" >> $GITHUB_OUTPUT

      - name: Installer Java 21
        if: steps.changes.outputs.changed == 'true'
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Compiler et tester
        if: steps.changes.outputs.changed == 'true'
        working-directory: ${{ matrix.service }}/backend
        run: mvn clean package -DskipTests=false

      # ── Docker Hub : uniquement sur main ──────────────────
      - name: Login Docker Hub
        if: steps.changes.outputs.changed == 'true' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build et Push image Backend
        if: steps.changes.outputs.changed == 'true' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}/backend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/ecopria-${{ matrix.service }}-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/ecopria-${{ matrix.service }}-backend:${{ github.sha }}

      - name: Afficher le résultat
        if: steps.changes.outputs.changed == 'false'
        run: echo "${{ matrix.service }} backend — aucune modification détectée, build ignoré"

  # ─────────────────────────────────────────────────────────
  #  FRONTEND — Angular de chaque microservice
  # ─────────────────────────────────────────────────────────
  build-frontend:
    name: Frontend ${{ matrix.service }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        service:
          - service-auth
          - service-utilisateur
          - service-action
          - service-inscription
          - service-presence
          - service-recompense
          - service-admin
          - service-notification

    steps:
      - name: Télécharger le code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Vérifier si le frontend a été modifié
        id: changes
        run: |
          git diff --name-only HEAD~1 HEAD | grep "^${{ matrix.service }}/frontend/" \
          && echo "changed=true" >> $GITHUB_OUTPUT \
          || echo "changed=false" >> $GITHUB_OUTPUT

      - name: Installer Node.js 18
        if: steps.changes.outputs.changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: ${{ matrix.service }}/frontend/package-lock.json

      - name: Installer les dépendances
        if: steps.changes.outputs.changed == 'true'
        working-directory: ${{ matrix.service }}/frontend
        run: npm ci

      - name: Compiler Angular
        if: steps.changes.outputs.changed == 'true'
        working-directory: ${{ matrix.service }}/frontend
        run: npm run build -- --configuration=production

      # ── Docker Hub : uniquement sur main ──────────────────
      - name: Login Docker Hub
        if: steps.changes.outputs.changed == 'true' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build et Push image Frontend
        if: steps.changes.outputs.changed == 'true' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}/frontend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/ecopria-${{ matrix.service }}-frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/ecopria-${{ matrix.service }}-frontend:${{ github.sha }}

      - name: Afficher le résultat
        if: steps.changes.outputs.changed == 'false'
        run: echo "${{ matrix.service }} frontend — aucune modification détectée, build ignoré"
/**
 * @deprecated Utiliser proxy.conf.local.js (npm start) ou proxy.conf.docker.js (npm run start:docker).
 * Conservé pour compatibilité ng serve sans script npm.
 */
module.exports = require('./proxy.conf.local.js');
